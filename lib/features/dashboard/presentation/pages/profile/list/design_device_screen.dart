import 'package:flutter/material.dart';

class DesignDeviceScreen extends StatelessWidget {
  const DesignDeviceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(children: []));
  }
}
